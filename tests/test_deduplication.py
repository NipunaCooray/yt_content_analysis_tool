"""Phase 3: deduplication of full-search results into the videos master list.

Not yet implemented -- see services/deduplication.py. Placeholder so the
test file structure from the handover doc (section 45) exists from the
start.
"""

import pytest


@pytest.mark.skip(reason="Deduplication service is implemented in Phase 3.")
def test_deduplication_placeholder():
    pass
